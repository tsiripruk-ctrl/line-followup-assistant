# v0.6.49 — Future Commitment Extraction & Task Context Isolation

ฐาน source: line-followup-assistant-v0.6.48.zip ที่ผู้ใช้ส่งมา

## สิ่งที่แก้

- ข้อความ “คาดว่าสามารถจัดส่งสินค้าอีกครั้งประมาณวันที่ 29-9-69 แต่รอจอยเฟิร์ม” เป็น PROGRESS_UPDATE ไม่ปิดงาน โดยเก็บ progress_summary ตามข้อความจริง, waiting_on = รอจอยยืนยัน, next_action = ยืนยันวันจัดส่งสินค้า
- 29-9-69 หมายถึง 29 กันยายน 2569 (2026) กำหนดติดตามเริ่ม 08:30 Asia/Bangkok หรือ 2026-09-29T01:30:00Z หากไม่มีเวลาระบุ ไม่แต่งข้อมูลความคืบหน้าที่ผู้ส่งไม่ได้กล่าว
- ปีสองหลัก 40–99 ใช้ พ.ศ. 2540–2599; 00–39 ใช้ ค.ศ. 2000–2039 รองรับปีสี่หลัก พ.ศ./ค.ศ. เช่นเดิม วันที่ไม่ถูกต้องหรือเวลาในอดีตไม่สร้างนัดอนาคต
- เก็บ checkpoint ใน OwnerPreference เดิมและคิวจริงใน Task.next_reminder_at; API แสดง next_followup_at เป็น UTC ISO 8601
- Reminder scan เคารพ checkpoint แม้เปิด forced follow-up หรือ force scan และแม้คิวถูกเลื่อนให้เร็วขึ้น ข้อความทั่วไป เช่น ยังรอ ไม่ลบวันนัด/สิ่งที่รอ เมื่อถึงนัดจึงกลับติดตามตามรอบเดิม
- คำถามติดตามใช้สิ่งที่ยังรอยืนยัน เช่น จอยยืนยันแล้วหรือยัง ไม่ย้อนถามวันส่งที่ได้รับแล้ว
- ค้นหา/ตอบคำถาม/เลือกงานอัปเดต จำกัดด้วย FU code หรือชื่อโครงการ/หัวข้องานจากข้อมูลตัวงาน ก่อนคะแนนความคล้าย ประวัติ และผู้รับผิดชอบ
- ข้อความระบุปากน้ำประแสไม่แสดงชุมแสงจากคำสรุปงานค้าง/วันนี้ ประวัติข้อความที่ปนกัน หรือชื่อผู้รับผิดชอบเดียวกัน
- FU ที่ไม่พบไม่ fallback ไปงานอื่น; หลายงานในโครงการเดียวกันแสดงตัวเลือกเมื่อยังระบุไม่ชัด; Reply ที่ระบุหัวข้อขัดกับงานที่อ้างอิงไม่แก้ทั้งสองงาน
- จำกัดการขยายคำพ้อง เช่น meter/มิเตอร์ ให้คำค้นเดี่ยว ป้องกันคำค้นหลายส่วนสูญเสียชื่อโครงการ

## การติดตั้งและข้อมูลเดิม

ไม่ต้องเพิ่ม environment variable หรือ database migration ใช้ตาราง OwnerPreference ที่มีอยู่แล้ว
คงคำสั่งบังคับติดตาม/ปิดบังคับติดตามและ health flags เดิม
การเลื่อนติดตามโดย owner พร้อมวันใหม่เป็นการแทน checkpoint เดิมอย่างชัดเจน

รุ่นนี้ไม่เขียนแก้ประวัติหรือวันนัดผิดในฐานข้อมูลเก่าย้อนหลังอัตโนมัติ งานที่เคยถูกบันทึกเป็นปี 2069 ให้ส่งอัปเดตใหม่โดย Reply งานนั้น หรือสั่งเลื่อนติดตาม FU พร้อมวันที่ที่ถูกต้อง
การแยกบริบทใช้ชื่อ/โครงการที่ตรงกับข้อมูลจริง หากเป็นชื่อเล่นที่ยังไม่รู้จักหรือข้อความกำกวม ควรระบุ FU หรือ Reply ข้อความของงาน

## ตรวจสอบ

- Python 3.13, dependencies จาก requirements.txt
- Compile ทุกไฟล์ Python: PASS
- Regression: 125 passed + 14 subtests passed (ชุดเดิม 97 กรณี + เพิ่ม 28 กรณี)
- HTTP GET /health ผ่าน FastAPI TestClient: 200, version 0.6.49
- flags: future_commitment_extraction=true, task_context_isolation=true
- ZIP CRC และความครบถ้วนเทียบ source: PASS
- มี deprecation warnings จาก datetime.utcnow และ lifecycle เดิม ไม่ใช่ test failures
- ทดสอบด้วยฐานข้อมูล SQLite แยกและจำลองการส่ง LINE; ยังไม่ได้ deploy หรือทดสอบกับ LINE/PostgreSQL production

รันซ้ำ: ติดตั้ง requirements.txt และ requirements-dev.txt แล้วใช้ python -m compileall -q . และ python -m pytest -q

## ทดสอบหลัง Deploy

1. GET /health ต้องเห็น version 0.6.49 และ flags สองรายการข้างต้น
2. Reply งานปากน้ำประแสด้วยข้อความตัวอย่าง โดยใช้วันนัดในอนาคต ตรวจ progress_summary/waiting_on/next_action และ next_followup_at
3. เปิดบังคับติดตาม แล้วส่ง ยังรอ: คิวต้องไม่เร็วกว่าวันนัด ตรวจว่าไม่มีข้อความติดตามส่งก่อนวันนัด
4. ค้นหา ปากน้ำประแส หรือ สรุปงานค้างปากน้ำประแสวันนี้: ต้องไม่แสดงชุมแสงหรือ FU อื่นนอกหัวข้อ
5. เมื่อถึงวันนัด ต้องถามสิ่งที่ยังรอยืนยัน และเมื่องานเสร็จต้องหยุด forced mode ตามเดิม
