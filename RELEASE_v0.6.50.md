# v0.6.50 — รับรู้และตอบรับอัปเดตจาก Reply งานเดิม

ต่อยอดจาก v0.6.49 สำหรับข้อความตัวอย่าง:
“ดำเนินการสั่งซื้ออุปกรณ์แล้วรอรับของส่งให้เกมดำเนินการครับ”

## สาเหตุที่พบใน source

1. ตัวจำแนกเดิมไม่รู้จักข้อความสั่งซื้อแล้วรอรับของ จึงอาจเรียก AI ก่อนถึงขั้นจับคู่ quotedMessageId; หาก AI ล้มเหลว เส้นทางนี้ออกก่อนบันทึก Reply
2. นโยบายตอบรับเดิมเงียบสำหรับ in_progress จึงไม่แสดงให้ผู้ตอบทราบว่าบันทึกแล้ว
3. การบันทึก snapshot เดิมยอมข้ามเมื่อผิดพลาด ทำให้สถานะกับรายละเอียดอัปเดตอาจไม่ครบ และการกันข้อความซ้ำจาก log อย่างเดียวขวางการลองใหม่หลังล้มเหลว

ภาพไม่สามารถยืนยันได้ว่าสาเหตุใดเกิดขึ้นจริงบน production ต้องใช้ version และ logs ประกอบ

## พฤติกรรมใหม่

- จับข้อความสั่งซื้อแล้วรอของเป็น PROGRESS_UPDATE / WAITING ในเครื่อง ไม่ต้องใช้ AI รวมข้อความที่มีเว้นวรรคหรือขึ้นบรรทัดกลางคำ
- Reply ที่ระบุงานเดิมเข้าการจับคู่ quotedMessageId ได้โดยไม่ต้องรอ AI; ถ้าเป็นข้อความทั่วไปที่ไม่รู้จักสถานะ บันทึกเป็น comment โดยไม่ล้างความคืบหน้าหรือเลื่อนคิว
- บันทึก progress_summary ตามข้อความ, waiting_on = รอรับอุปกรณ์ที่สั่งซื้อ, next_action = รับของแล้วส่งให้เกมดำเนินการ
- ไม่ปิดงานและไม่เปลี่ยนผู้รับผิดชอบเป็นเกมอัตโนมัติ
- หลัง commit สำเร็จ ตอบยืนยันชื่อเรื่อง สิ่งที่รอ ขั้นตอนถัดไป และเวลาติดตาม คำตอบนี้ผูกกับงานเดิมเพื่อ Reply ต่อได้
- คำถามติดตามต่ออ้างอิงการรอรับของ ไม่กลับไปถามความคืบหน้าแบบเริ่มต้น
- ถ้า quotedMessageId ไม่ตรงกับงานที่หาได้ จะแจ้งให้ระบุงาน/Reply ข้อความติดตามล่าสุด ไม่ใช้ความใหม่ของข้อความไปเดางานอื่น
- snapshot กับสถานะบันทึกใน transaction เดียว หากล้มเหลว rollback และแจ้งว่ายังไม่สำเร็จ ข้อความเดิมที่บันทึกไม่สำเร็จสามารถลองใหม่ได้; ข้อความที่บันทึกสำเร็จแล้วส่งซ้ำไม่อัปเดตซ้ำ
- คงการแยกบริบทและการเคารพวันนัดจาก v0.6.49

## ผลตรวจ

- 134 tests passed + 14 subtests passed (เพิ่ม 9 กรณีจาก v0.6.49)
- ทดสอบเส้นทาง process_message ด้วยข้อความจากภาพ ตั้งให้ AI ใช้ไม่ได้: บันทึกและตอบรับสำเร็จ
- ทดสอบข้อความซ้ำ, Reply ไม่พบงาน, จำลอง snapshot ล้มเหลวและลองใหม่, คำถาม, comment, และ health flags
- Compile PASS; HTTP GET /health 200; ZIP integrity PASS
- VERSION 0.6.50, quoted_progress_receipt=true, procurement_waiting_update=true
- ไม่เพิ่ม database migration หรือ environment variable
- ใช้ SQLite และจำลอง LINE ในการทดสอบ ยังไม่ได้ deploy/ทดสอบ LINE หรือ PostgreSQL production

## หลังติดตั้ง

1. Deploy v0.6.50 และตรวจ /health ว่า version กับ flags ตรง
2. ให้ตี๋ Reply ข้อความติดตามล่าสุดของงานเดิม แล้วส่งข้อความจากภาพอีกครั้ง ข้อความเก่าไม่ได้ประมวลผลย้อนหลังอัตโนมัติ
3. ต้องเห็นเลขาตอบรับ รอรับอุปกรณ์ → รับของแล้วส่งให้เกมดำเนินการ พร้อมรอบติดตามถัดไป
4. ตรวจ dashboard ว่างานยัง WAITING และผู้รับผิดชอบยังเป็นตี๋
5. หากยังไม่รับรู้ ให้ตรวจ log ที่มี [QUOTED_TASK_RESOLVED] หรือ [QUOTED_TASK_UNRESOLVED] และ quotedMessageId ของเหตุการณ์นั้น เพื่อแยกปัญหา webhook/ฐานข้อมูล/ข้อความอ้างอิง
